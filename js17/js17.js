// ブラウザがフリーズする例（重い計算）
for (let i = 0; i < 1e9; i++) {
  // ただ回すだけでも重い
}

console.log("終わった！");
// ブラウザがフリーズする例（重い計算）
for (let i = 0; i < 1e9; i++) {
  // ただ回すだけでも重い
}

console.log("終わった！"); // ブラウザがフリーズする例（重い計算）
for (let i = 0; i < 1e9; i++) {
  // ただ回すだけでも重い
}

console.log("終わった！"); // ブラウザがフリーズする例（重い計算）
for (let i = 0; i < 1e9; i++) {
  // ただ回すだけでも重い
}

console.log("終わった！");
